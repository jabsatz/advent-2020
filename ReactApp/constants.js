const OPTION_TYPE = {
  RUN: 'run',
  TEST: 'test',
  NEW: 'new',
};

const PHASE_TYPE = {
  OPTIONS: 'options',
  DAY: 'day',
  IN_PROGRESS: 'in-progress',
};

module.exports = { OPTION_TYPE, PHASE_TYPE };
